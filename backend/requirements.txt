fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
redis==5.0.8
python-dotenv==1.0.1
alembic==1.13.2
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.9.0
python-multipart==0.0.9
email-validator==2.2.0
aiofiles==24.1.0
# httpx is pinned HERE (not only in requirements-dev.txt) because openai
# and anthropic both depend on it transitively, and httpx 0.28 removed the
# `proxies` argument that openai 1.51 still passes. Leaving it unpinned in
# production meant tests (which install requirements-dev.txt with a pinned
# httpx) passed while the production image resolved 0.28.1 and crashed at
# runtime. Any transitive dependency that can break the build belongs in
# the production pin list.
httpx==0.27.2
openai==1.51.0
anthropic==0.36.1
pypdf==5.0.1
python-docx==1.1.2
reportlab==4.2.5
qdrant-client==1.12.0
langgraph==0.2.39
